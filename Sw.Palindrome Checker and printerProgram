/*
Problem 2: Palindrome Checker and Printer 🔄

Goal: Write a program that takes a string as input, checks if it is a palindrome (reads the same forwards and backward), and prints a specific message based on the result. It must also print the reverse of the original string for comparison.
Input Example 1: madam
Expected Output 1:
Original String: madam
Reversed String: madam
Result: The string is a Palindrome.
Input Example 2: level
Expected Output 2:
Original String: level
Reversed String: level
Result: The string is a Palindrome.
Input Example 3: hello
Expected Output 3:
Original String: hello
Reversed String: olleh
Result: The string is NOT a Palindrome.
Key Concepts:
String reversal (using loops or built-in functions/slicing).
String comparison.
*/

var word : String = "hello"

let reversedword = String(word.reversed())

  if word == reversedword {
    
	  print("Original String: \(word)")
    print("Reversed String: \(reversedword)")
	  print("Result: The string (\(word)) is a Palindrome.")
        
  }else {
		
    print("Original String: \(word)")
    print("Reversed String: \(reversedword)")
	  print("Result: The string (\(word)) is NOT a Palindrome.")

}

